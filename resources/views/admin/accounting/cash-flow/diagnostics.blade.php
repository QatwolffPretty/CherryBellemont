<x-layouts.admin title="Cash Flow Diagnostics | Cherry Bellemont"><x-admin.section width="7xl"><x-admin.page-header eyebrow="Accounting · Cash Flow" title="Cash Flow Diagnostics" subtitle="Read-only checks highlight configuration or classification gaps. No historical journal data is changed automatically."><x-slot:actions><x-admin.button variant="outline" :href="route('admin.accounting.cash-flow.index', $filters)" icon="bi-arrow-left">Cash Flow Overview</x-admin.button></x-slot:actions></x-admin.page-header><x-admin.card class="mt-8" title="Diagnostics"><x-admin.table class="mt-5"><x-slot:head><tr><th>Severity</th><th>Issue</th><th>Explanation</th><th class="text-right">Journal</th></tr></x-slot:head>
@forelse($issues as $issue)<tr><td><x-admin.badge :status="$issue['severity']" :label="str($issue['severity'])->title()" /></td><td class="font-semibold">{{ $issue['title'] }}</td><td class="text-cream/70">{{ $issue['description'] }}</td><td class="text-right">@if($issue['journal'])<a class="text-gold hover:text-cream" href="{{ route('admin.accounting.journals.show', $issue['journal']) }}">{{ $issue['journal']->entry_number }}</a>@elseif($issue['account'])<a class="text-gold hover:text-cream" href="{{ route('admin.accounting.accounts.show', $issue['account']) }}">{{ $issue['account']->code }}</a>@else<span class="text-cream/50">—</span>@endif</td></tr>@empty<tr><td colspan="4" class="py-10 text-center text-cream/60">No Cash Flow diagnostics were found for this period.</td></tr>@endforelse</x-admin.table></x-admin.card></x-admin.section></x-layouts.admin>
