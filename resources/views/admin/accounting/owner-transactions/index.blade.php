<x-layouts.admin title="Owner Compensation | Cherry Bellemont"><x-admin.section width="7xl"><x-admin.page-header eyebrow="Accounting" title="Owner Compensation" subtitle="Owner salary is an operating expense. Drawings and reserve allocations are equity movements."><x-slot:actions><x-admin.button :href="route('admin.accounting.owner-transactions.create')" icon="bi-plus-lg">New Transaction</x-admin.button></x-slot:actions></x-admin.page-header><x-admin.card class="mt-8"><x-admin.table><x-slot:head><tr><th>Date</th><th>Reference</th><th>Type</th><th>Amount</th><th>Status</th><th>Journal</th><th></th></tr></x-slot:head>@forelse($transactions as $transaction)<tr><td>{{ $transaction->transaction_date?->format('d M Y') }}</td><td>{{ $transaction->transaction_number }}</td><td>{{ str($transaction->transaction_type)->replace('_',' ')->title() }}</td><td>RM {{ number_format($transaction->amount,2) }}</td><td><x-admin.badge :value="$transaction->status" /></td><td>@if($transaction->journalEntry)<a class="text-gold" href="{{ route('admin.accounting.journals.show',$transaction->journalEntry) }}">{{ $transaction->journalEntry->entry_number }}</a>@else — @endif</td><td>@if($transaction->status==='draft')<a class="text-gold" href="{{ route('admin.accounting.owner-transactions.edit',$transaction) }}">Edit</a>@endif</td></tr>@empty<tr><td colspan="7" class="text-cream/60">No owner compensation records yet.</td></tr>@endforelse</x-admin.table><div class="mt-5">{{ $transactions->links() }}</div></x-admin.card></x-admin.section></x-layouts.admin>
