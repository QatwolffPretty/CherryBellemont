<x-layouts.store title="Collection admin | Cherry Bellemont"><section class="mx-auto max-w-6xl px-6 py-16"><div class="flex items-center justify-between"><h1 class="text-4xl">Collection</h1><a href="{{ route('admin.products.create') }}" class="luxury-link">Add piece</a></div>@if(session('success'))<p class="mt-6 border border-gold/50 p-4 text-gold">{{ session('success') }}</p>@endif<div class="mt-8 overflow-x-auto"><table class="w-full text-left"><thead class="border-b border-cream/20 text-sm uppercase tracking-widest text-gold"><tr><th class="p-3">Piece</th><th class="p-3">Price</th><th class="p-3">Status</th><th></th></tr></thead><tbody>@foreach($products as $product)<tr class="border-b border-cream/10"><td class="p-3">{{ $product->name }}</td><td class="p-3">RM {{ number_format($product->price,2) }}</td><td class="p-3 capitalize">{{ $product->status }}</td><td class="p-3 text-right"><a class="text-gold" href="{{ route('admin.products.edit',$product) }}">Edit</a><form class="ml-4 inline" method="POST" action="{{ route('admin.products.destroy', $product) }}" onsubmit="return confirm('Remove this piece from the collection?')">@csrf @method('DELETE')<button class="text-cream/60 transition hover:text-gold" type="submit">Delete</button></form></td></tr>@endforeach</tbody></table></div>{{ $products->links() }}</section></x-layouts.store>
