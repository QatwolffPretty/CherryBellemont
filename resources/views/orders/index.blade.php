<x-layouts.store title="My orders | Cherry Bellemont"><section class="mx-auto max-w-5xl px-6 py-16"><p class="uppercase tracking-[.25em] text-gold">Your account</p><h1 class="mt-3 text-5xl">My orders</h1><div class="mt-10 space-y-4">@forelse($orders as $order)<a class="flex items-center justify-between border border-cream/15 p-5 transition hover:border-gold" href="{{ route('orders.show', $order) }}"><span><strong>{{ $order->order_number ?? $order->number }}</strong><span class="ml-3 capitalize text-cream/60">{{ $order->order_status ?? $order->status }}</span></span><span class="text-gold">RM {{ number_format($order->total,2) }}</span></a>@empty<p class="text-cream/65">You have not placed an order yet.</p>@endforelse</div><div class="mt-8">{{ $orders->links() }}</div></section></x-layouts.store>
