<x-layouts.store :title="($order->order_number ?? $order->number).' | Cherry Bellemont'"><section class="mx-auto max-w-5xl px-6 py-16"><p class="uppercase tracking-[.25em] text-gold">Order</p><h1 class="mt-3 text-4xl">{{ $order->order_number ?? $order->number }}</h1><div class="mt-8 grid gap-8 md:grid-cols-2"><div class="space-y-4">@foreach($order->items as $item)<div class="flex justify-between border-b border-cream/15 pb-4"><span>{{ $item->product_name ?? $item->name }} × {{ $item->quantity }}</span><span>RM {{ number_format($item->line_total ?? $item->total,2) }}</span></div>@endforeach</div><aside class="border border-cream/15 p-6"><p>Payment: <span class="capitalize text-gold">{{ $order->payment_method }}</span></p><p class="mt-3">Payment status: <span class="capitalize text-gold">{{ $order->payment_status ?? 'pending' }}</span></p><p class="mt-3">Order status: <span class="capitalize text-gold">{{ $order->order_status ?? $order->status }}</span></p><p class="mt-6 text-2xl">Total <span class="float-right text-gold">RM {{ number_format($order->total,2) }}</span></p></aside></div></section></x-layouts.store>
