<x-layouts.store title="Collection | Cherry Bellemont"><section class="mx-auto max-w-7xl px-6 py-20"><p class="uppercase tracking-[.25em] text-gold">The collection</p><h1 class="mt-3 text-5xl">Quiet distinction.</h1><div class="mt-14 grid gap-8 sm:grid-cols-2 lg:grid-cols-3">@foreach($products as $product)<article class="group"><a href="{{ route('products.show', $product) }}"><div class="aspect-[3/4] overflow-hidden border border-gold/20 bg-wine-deep">@if($product->image_path)<img class="h-full w-full object-cover transition duration-500 group-hover:scale-[1.03]" src="{{ asset('storage/' . $product->image_path) }}" alt="{{ $product->name }}">@else<div class="flex h-full items-center justify-center text-gold/70">CHERRY BELLEMONT</div>@endif</div><h2 class="mt-4 text-2xl">{{ $product->name }}</h2></a><div class="mt-1 flex items-center justify-between"><p class="text-gold">RM {{ number_format($product->price, 2) }}</p><form method="POST" action="{{ route('cart.store', $product) }}">@csrf <input type="hidden" name="quantity" value="1"><button class="nav-link" type="submit" @disabled($product->stock < 1)>{{ $product->stock > 0 ? 'Add to bag' : 'Sold out' }}</button></form></div></article>@endforeach</div><div class="mt-12">{{ $products->links() }}</div></section></x-layouts.store>
