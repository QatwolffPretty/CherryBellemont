<x-layouts.store title="Cherry Bellemont | Real power, in silence.">
    <section class="relative overflow-hidden border-b border-gold/30 bg-wine py-32 text-center md:py-48"><img src="{{ asset('images/cherry-mono.png') }}" alt="" class="pointer-events-none absolute inset-0 m-auto h-full w-2/3 object-contain opacity-10"><div class="relative mx-auto max-w-3xl px-6"><p class="mb-5 uppercase tracking-[.35em] text-gold">The House of Cherry Bellemont</p><h1 class="text-5xl leading-tight md:text-7xl">Real power,<br>in silence.</h1><p class="mx-auto mt-7 max-w-lg text-2xl text-cream/80">Designed not to be seen. Designed to be remembered.</p><a href="{{ route('collection') }}" class="luxury-link mt-10 inline-block">Enter the collection</a></div></section>
    <section class="bg-cream px-6 py-24 text-center text-wine"><p class="uppercase tracking-[.28em] text-gold">A study in restraint</p><h2 class="mt-5 text-4xl md:text-5xl">Marked, not branded.</h2></section>
    <section class="bg-wine px-6 py-24"><div class="mx-auto max-w-6xl"><div class="mb-12 flex items-end justify-between"><div><p class="uppercase tracking-[.25em] text-gold">The edit</p><h2 class="mt-3 text-4xl">Selected pieces</h2></div><a href="{{ route('collection') }}" class="nav-link">View all</a></div><div class="grid gap-8 md:grid-cols-3">@forelse($featuredProducts as $product)<article class="group"><a href="{{ route('products.show', $product) }}"><div class="aspect-[3/4] overflow-hidden border border-gold/20 bg-wine-deep">@if($product->primaryImagePath())<img src="{{ asset('storage/' . $product->primaryImagePath()) }}" alt="{{ $product->name }}" class="h-full w-full object-cover transition duration-500 group-hover:scale-[1.03]">@else<div class="flex h-full items-center justify-center text-gold/70">CHERRY BELLEMONT</div>@endif</div><h3 class="mt-5 text-2xl">{{ $product->name }}</h3></a><div class="mt-2 flex items-center gap-2 text-sm text-cream/70"><x-reviews.stars :rating="$product->approved_reviews_avg_rating ?? 0" size="text-xs" /><span>({{ $product->approved_reviews_count }})</span></div><div class="mt-2 flex justify-between"><p class="text-gold">RM {{ number_format($product->price, 2) }}</p>@if($product->variants_count > 0)<a class="nav-link" href="{{ route('products.show', $product) }}">Choose options</a>@else<form method="POST" action="{{ route('cart.store', $product) }}">@csrf <input type="hidden" name="quantity" value="1"><button class="nav-link" type="submit" @disabled($product->stock < 1)>{{ $product->stock > 0 ? 'Add to bag' : 'Sold out' }}</button></form>@endif</div></article>@empty <p class="col-span-3 py-10 text-center text-cream/65">The first collection is being prepared.</p>@endforelse</div></div></section>
</x-layouts.store>
