<x-layouts.store :title="$product->name.' | Cherry Bellemont'"><section class="mx-auto grid max-w-6xl gap-12 px-6 py-16 md:grid-cols-2"><div class="aspect-[3/4] overflow-hidden border border-gold/20 bg-wine-deep">@if($product->image_path)<img class="h-full w-full object-cover" src="{{ asset('storage/' . $product->image_path) }}" alt="{{ $product->name }}">@else<div class="flex h-full items-center justify-center border border-cream/10 text-center"><span class="font-display text-2xl tracking-[.2em] text-gold/70">CHERRY<br>BELLEMONT</span></div>@endif</div><div class="flex flex-col justify-center"><p class="uppercase tracking-[.25em] text-gold">Cherry Bellemont</p><h1 class="mt-4 text-5xl">{{ $product->name }}</h1><p class="mt-4 text-2xl text-gold">RM {{ number_format($product->price, 2) }}</p><p class="mt-8 whitespace-pre-line text-xl leading-relaxed text-cream/85">{{ $product->description }}</p><form class="mt-10 flex items-center gap-4" method="POST" action="{{ route('cart.store', $product) }}">@csrf <input class="field w-20" type="number" name="quantity" min="1" max="{{ $product->stock }}" value="1" @disabled($product->stock < 1)><button class="luxury-link" type="submit" @disabled($product->stock < 1)>{{ $product->stock > 0 ? 'Add to bag' : 'Sold out' }}</button></form>@error('quantity')<p class="mt-3 text-gold">{{ $message }}</p>@enderror<p class="mt-5 text-sm text-cream/55">{{ $product->stock }} currently available.</p></div></section></x-layouts.store>
